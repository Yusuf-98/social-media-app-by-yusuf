import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Post — Sociality",
};

export default function CreatePostLayout({ children }: { children: React.ReactNode }) {
  return children;
}
